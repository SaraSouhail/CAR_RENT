<div class="navbar navbar-expand-lg navbar-light navbar-lewagon bg-light fixed-top">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <span class="text-warning">Car</span>Rent
    <% end %>
     <button aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-bs-target="#navbarSupportedContent" data-bs-toggle="collapse" type="button">
     <span class="navbar-toggler-icon"></span>
     </button>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item active">
            <%= link_to "Home", "#", class: "nav-link" %>
          </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Cars", cars_path, class: "nav-link" %>
          </li>

          <li class="nav-item dropdown">
          <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <div> <% if current_user.profile_photo.key.nil?%>

          <img class="avatar" src="https://kitt.lewagon.com/placeholder/users/ssaunier" alt="car-image">
          <% else %>
          <%= cl_image_tag current_user.profile_photo.key, class:"avatar" %>
          <%end%></div>
          </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <% if current_user.role == "owner" %>
              <%= link_to "Profile", owner_path, class: " dropdown-item nav-link p-3" %>
            <% end %>
            <% if current_user.role == "client" %>
              <%= link_to "Profile", client_path, class: " dropdown-item nav-link p-3" %>
            <% end %>
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item p-3" %>
            </div>
          </li>
        <% else %>
					<%# <li class="nav-item">
						<a class="nav-link" href="#team">Team</a>
					</li> %>
          <li class="nav-item">
            <a class="nav-link" href="#about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#services">Services</a>
          </li>
          <%# <li class="nav-item">
            <a class="nav-link" href="#team">Team</a>
          </li> %>
          <li class="nav-item">
            <a class="nav-link" href="#contact">Contact</a>
          </li>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
